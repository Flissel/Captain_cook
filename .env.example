# Only required for the legacy API-key-backed prototype, never for `python main.py demo`.
OPENAI_API_KEY=
CAPTAIN_MODEL=gpt-5.6
N8N_MCP_URL=http://localhost:15678/mcp-server/http
N8N_MCP_TOKEN=
CONTEXT7_API_KEY=

# Existing VibeMind n8n instance (Captain Cook does not own its container or volume).
N8N_URL=http://localhost:15678
N8N_CONTAINER_URL=http://host.docker.internal:15678

# Captain Cook delivery services.
GENERIC_TIMEZONE=Europe/Berlin
MAILPIT_WEB_PORT=8025
MAILPIT_SMTP_PORT=1025
MAILPIT_URL=http://localhost:8025
MAILPIT_SMTP_HOST=host.docker.internal
MARIADB_PORT=3306
MARIADB_DATABASE=ledger
MARIADB_USER=captain
MARIADB_PASSWORD=replace-with-a-long-random-password
MARIADB_ROOT_PASSWORD=replace-with-a-different-long-random-password
