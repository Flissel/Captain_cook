# CodebaseInventoryV1

Return the released typed envelope with the invocation bindings plus inspected
revision, content-addressed observations, reusable components, assertion/test
mappings, documentation provenance, tool matches, gaps, uncertainties, and
conflicts. Store references and digests, never source bodies, secret values,
absolute user paths, or raw terminal output.
