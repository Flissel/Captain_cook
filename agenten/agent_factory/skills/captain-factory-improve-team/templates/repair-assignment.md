# Repair Assignment

- Bind the released improvement request, failed assertion IDs, evidence refs,
  candidate parent digest, complete prior green regression set, lease, attempt,
  and remaining cost/time budget.
- Select the smallest evidence-implicated `changed_components` enum set and keep
  the supplied prior-green guards unchanged.
- Return only the digest-bound improvement attestation. Captain creates the
  child-candidate references and requires later tests, manifests, digests, and
  command evidence. Do not promote or weaken assertions.
