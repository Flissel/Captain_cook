# TeamExecutionEvidenceV1

Bind invocation, candidate, prompt, schema, tool, and workflow digests. Record
preflight results, released case reference, conversation/handoff observations,
typed tool evidence, termination, assertion observations, timing, and redacted
provider cost receipt. Require n8n scoped-tool reference, workflow digest, and
execution ID when integration work is declared. Preserve failures and skips.
