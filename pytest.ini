[pytest]
testpaths = tests
addopts =
    --cov=agenten
    --cov=blockchain
    --cov=gateway
    --cov-report=term-missing
    --cov-fail-under=70
markers =
    live: requires real local or external services and never skips when selected
