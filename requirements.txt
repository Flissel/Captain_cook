# Pinned dependencies for rap_project.
#
# Event-driven supply-chain subsystem (agenten/events, agenten/runtime,
# agenten/decomposition, agenten/constitution, agenten/spawning,
# agenten/ledger_bridge, ...) targets AutoGen 0.7.5:
autogen-core==0.7.5
autogen-agentchat==0.7.5
# `[openai]` extra pulls in `openai`, `tiktoken`, `aiofiles` -- required by
# `autogen_ext.models.openai.OpenAIChatCompletionClient`, used by
# `agenten/llm/model_client.py::build_model_client` (unit: real LLM-backed
# decompose/judge implementations).
autogen-ext[openai]==0.7.5

# Existing agenten/ and blockchain/ dependencies (previously unpinned).
pydantic==2.13.4
python-dotenv==1.0.1
plotly==6.9.0
networkx==3.6.1
sentence-transformers==5.6.0
beautifulsoup4==4.15.0
aiohttp==3.14.1
selenium==4.46.0

# The legacy `pyautogen` 0.2 distribution is intentionally not supported.
# Compatibility entry points use current AgentChat classes or plain Python
# services, so a clean install has one AutoGen API surface.
